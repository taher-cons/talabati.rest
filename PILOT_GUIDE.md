# دليل التجربة الفعلية — مطعم فراس (أول مطعم حقيقي)
**الموقع:** https://talabati.rest · **لوحة الإدارة:** https://talabati.rest/admin

---

## القسم صفر: الجواب على سؤالك المعماري (اقرأه أولاً)

سألت: **نظام دخول، أم نسخة مستقلة لكل مطعم؟**

**الجواب: نظام دخول واحد ونسخة واحدة. لا تُنشئ نسخاً منفصلة.**

السبب أن قاعدة البيانات **مبنيّة أصلاً لتعدد المطاعم**: جدول `restaurants` فيه `slug`،
وكل مطعم له قوائمه وأطباقه، والروابط العامة مبنية على الـ slug:

```
talabati.rest/menu/firas      → قائمة مطعم فراس
talabati.rest/menu/restaurant-2 → مطعم آخر، نفس النسخة، نفس الكود
```

| | نسخة واحدة (موصى به) | نسخة لكل مطعم |
|---|---|---|
| التكلفة | مشروع Firebase + Supabase واحد | ×4 مشاريع، ×4 فواتير |
| إصلاح خطأ | مرة واحدة للجميع | 4 مرات، ونسيان واحدة = مطعم معطوب |
| إضافة مطعم خامس | 5 دقائق من اللوحة | نشر مشروع كامل جديد |
| نطاق مخصص لكل مطعم | ممكن لاحقاً (توجيه نطاق → نفس النسخة) | ممكن |

النسخ المستقلة تبدو "أأمن" لكنها في الواقع أسوأ: 4 قواعد بيانات لن تُحدّث بنفس الوتيرة،
وستجد نفسك بعد شهرين مع 4 إصدارات مختلفة من نفس التطبيق.

### لكن هناك فجوة يجب إغلاقها قبل إعطاء أصحاب المطاعم حسابات

النظام الآن يعرف **الأدوار** (`owner`/`admin`/`manager` في جدول `user_profiles`)
ويعرف **أي مطعم** يتبع كل مستخدم (`restaurant_id`)، لكن التحقق الحالي في
`functions/src/middleware/auth.js` يتأكد أن المستخدم **صاحب مطعم** — ولا يتأكد أنه
صاحب **هذا المطعم بالتحديد**.

> بصيغة أخرى: لو أعطيت صاحب مطعم فراس حساباً اليوم، سيستطيع تقنياً تعديل قائمة
> المطاعم الثلاثة الأخرى. هذا مقبول تماماً في تجربة مطعم واحد تُديرها أنت،
> وغير مقبول عند إضافة أصحاب آخرين.

**لذلك الخطة على مرحلتين:**

| المرحلة | من يدخل | ما نحتاجه |
|---|---|---|
| **1 — تجربة فراس (الآن)** | أنت فقط، بمفتاح الإدارة | لا شيء، النظام جاهز |
| **2 — قبل المطاعم الثلاثة** | كل صاحب مطعم بحسابه | شاشة دخول + عزل البيانات لكل مطعم (تفاصيل في القسم 7) |

لا تُعطِ أحداً حساباً قبل إتمام المرحلة 2.

---

## القسم 1: ما نطلبه من مطعم فراس (قائمة تحضيرية)

أرسل له هذه القائمة حرفياً:

### أ) بيانات المطعم
- [ ] الاسم بالعربية والإنجليزية
- [ ] الشعار (Logo) — PNG بخلفية شفافة، 512×512 على الأقل
- [ ] وصف قصير (سطران) بالعربية والإنجليزية
- [ ] الهاتف، العنوان، البريد
- [ ] روابط التواصل (إنستغرام/فيسبوك) — اختياري
- [ ] لونان يمثلان هويته البصرية (Primary + Accent) بصيغة `#RRGGBB`
- [ ] أوقات العمل

### ب) الأطباق
**ابدأ بـ 8–12 طبقاً فقط، لا القائمة كاملة.** اختر:
- أكثر 5 أطباق مبيعاً (لأنها الأكثر مشاهدة)
- 3 أطباق مرتفعة السعر (لأن ثلاثية الأبعاد تبيعها فعلاً)
- 2 طبقاً جديداً يريد الترويج لهما

لكل طبق:
- [ ] الاسم (عربي + إنجليزي)
- [ ] الوصف (عربي + إنجليزي)
- [ ] السعر + العملة
- [ ] التصنيف (مقبلات / أطباق رئيسية / حلويات / مشروبات)
- [ ] صورة احترافية (للـ thumbnail) — JPG ≥ 1000px
- [ ] القيم الغذائية إن توفّرت (اختياري)
- [ ] **موديل ثلاثي الأبعاد** ← الجزء الصعب، انظر القسم 2

---

## القسم 2: الموديلات ثلاثية الأبعاد (أهم وأصعب خطوة)

هذا هو العائق الحقيقي في المشروع، وليس البرمجة. أمامك ثلاثة طرق:

### الطريقة أ) المسح بالهاتف — **الموصى بها للتجربة**
مجانية تقريباً وتنتج نتيجة واقعية لأنها الطبق الحقيقي.

1. حمّل تطبيق **Polycam** أو **RealityScan** (من Epic) — كلاهما على iOS وأندرويد.
2. ضع الطبق على طاولة **بيضاء أو رمادية سادة**، بإضاءة منتشرة (لا فلاش، لا ظل قوي).
3. صوّر **دورة كاملة 360°** حول الطبق: 30–40 صورة، ثلاث ارتفاعات
   (مستوى الطبق، 45°، من الأعلى).
4. اترك التطبيق يعالج، ثم **Export → GLB**.
5. الأطباق اللامعة أو الشفافة (الشوربة، المشروبات، الزجاج) تفشل عادةً — لهذه
   استخدم الطريقة (ج).

⏱ الوقت الواقعي: **15–25 دقيقة لكل طبق** بين التصوير والمعالجة والتنظيف.
أي 10 أطباق ≈ 4 ساعات عمل. خطّط لجلسة تصوير واحدة في المطعم.

### الطريقة ب) شراء موديلات جاهزة
`sketchfab.com` / `cgtrader.com` / `turbosquid.com` — ابحث بـ "food GLB".
سريع (دقائق) لكن الطبق **لن يشبه طبق المطعم**، وهذا يضرّ بالمصداقية.
مناسب فقط للمشروبات المعلّبة والأشياء القياسية.

### الطريقة ج) تصميم يدوي
Blender بيد مصمم. أفضل نتيجة وأغلى تكلفة (20–60$ للطبق).

### المواصفات الإلزامية (مهم — تجاوزها يُبطئ التطبيق على الهاتف)

| المعيار | الحد | السبب |
|---|---|---|
| الصيغة | `.glb` (وليس `.gltf` + ملفات) | ملف واحد، تحميل أسرع |
| الحجم | **أقل من 5 ميغابايت** (الحد التقني 50MB) | هاتف على شبكة الجيل الرابع |
| المضلّعات | < 100 ألف مثلث | حرارة الهاتف والأداء |
| النسيج (Texture) | 1024×1024 أو 2048 كحد أقصى | الذاكرة |
| المقياس | بالأمتار: الطبق ≈ **0.25 م** عرضاً | يظهر بحجمه الحقيقي على الطاولة |
| نقطة الأصل | في **قاعدة** الطبق، لا مركزه | لا يغوص في الطاولة |
| الاتجاه | +Y إلى الأعلى | معيار glTF |

### ضغط الموديل قبل الرفع (خطوة لا تتجاهلها)
```bash
npx @gltf-transform/cli optimize dish.glb dish-optimized.glb --texture-size 1024
```
تقلّص الحجم عادةً **60–80%** بلا فرق مرئي. التطبيق يدعم ضغط DRACO أصلاً.

---

## القسم 3: إدخال البيانات (خطوة بخطوة)

1. **افتح** https://talabati.rest/admin
2. ستطلب منك اللوحة **مفتاح الإدارة** — الصقه (`ADMIN_API_KEY` من ملف `.env`).
   يُحفظ في `sessionStorage` فقط، أي يُنسى بإغلاق التبويب.
3. **أنشئ المطعم:** الاسم، الشعار، الألوان، وحدّد `slug` = `firas`
   ← هذا يحدد الرابط النهائي: `talabati.rest/menu/firas`
   الـ slug: حروف إنجليزية صغيرة وشرطات فقط، ولا يُغيَّر بعد طباعة رمز QR.
4. **⚠️ فعّل "Published"** — وإلا لن يراه الزبائن (الـ API يُظهر المنشور فقط).
5. **أنشئ القائمة** (Menu) واربطها بالمطعم، وفعّل "Active".
6. **أضف التصنيفات** بالترتيب الذي تريده على الشاشة.
7. **أضف الأطباق:** لكل طبق ارفع `GLB` + الصورة المصغّرة، وأدخل السعر والوصف.
8. **احصل على رمز QR للطباعة.** لا يوجد زر QR في اللوحة حالياً (تحقّقت من ذلك)،
   لكن الـ API يُنشئه. نفّذ في الطرفية بعد استبدال `MENU_ID`:
   ```bash
   curl.exe -s https://talabati.rest/api/menu/qr/MENU_ID
   ```
   يُرجع رابط صورة QR تُشير إلى `talabati.rest/ar/MENU_ID`. احفظها واطبعها.
   بديل أبسط: أنشئ QR لرابط `https://talabati.rest/menu/firas` من أي مولّد QR
   — وهذا **أفضل للطاولات**، لأنه يوصل الزبون إلى القائمة كاملة لا إلى طبق واحد.


---

## القسم 4: الاختبار قبل الذهاب إلى المطعم

اختبر على **هاتف حقيقي**، لا على متصفح الحاسوب — الكاميرا و AR لا تُحاكى بصدق.

```
1. talabati.rest/menu/firas        → تظهر القائمة والأطباق والأسعار
2. اضغط "View in AR" على طبق       → يطلب إذن الكاميرا → اسمح
3. وجّه الكاميرا إلى طاولة          → تظهر حلقة الاستهداف
4. اضغط لتثبيت الطبق               → يظهر الموديل بحجمه الحقيقي
5. جرّب: إصبعان للتصغير/التكبير، إصبع للدوران
6. اضغط زر الكاميرا 📷              → التقاط صورة ومشاركتها
```

**قائمة تحقق:**
- [ ] أندرويد + Chrome (الأغلبية)
- [ ] آيفون + **Safari** (على iOS، Chrome لا يملك وصولاً كاملاً للكاميرا)
- [ ] شبكة الجيل الرابع لا واي فاي (زمن التحميل الحقيقي)
- [ ] حجم الطبق منطقي — لا عملاق ولا مجهري
- [ ] الطبق لا يغوص في الطاولة ولا يطفو فوقها
- [ ] الأسعار والأسماء العربية صحيحة (والاتجاه RTL سليم)
- [ ] طبق **بلا** موديل ثلاثي الأبعاد → يجب أن يظهر طبق بديل ورسالة واضحة، لا شاشة فارغة

> إن ظهر الموديل بحجم خاطئ: صحّح `scale` في بيانات الطبق من اللوحة بدل إعادة التصدير.

---

## القسم 5: التجربة الميدانية في المطعم

**متى:** ساعة هادئة، لا وقت الذروة (لتكون الأخطاء أقل تكلفة).

**جهّز:**
- طاولة واحدة عليها رمز QR مطبوع (Standee صغير أفضل من ملصق)
- هاتفين للتجربة (أندرويد + آيفون)
- ورقة لتسجيل الملاحظات

**ما يجب مراقبته وكتابته:**
| المؤشر | كيف تقيسه |
|---|---|
| هل فهم الزبون ماذا يفعل بلا شرح؟ | راقب دون تدخل، وسجّل عدد من احتاج شرحاً |
| كم ثانية حتى ظهور الطبق؟ | ساعة إيقاف — إن تجاوز 10 ثوانٍ فالموديل ثقيل |
| هل نجح تثبيت السطح؟ | الإضاءة الخفيفة والطاولات اللامعة السوداء تُفشل التتبّع |
| ماذا قال الزبون؟ | اقتباسات حرفية، لا تلخيص |
| هل غيّر رأيه في الطلب؟ | اسأل النادل: هل طَلَب ما رآه؟ |

**السؤال الحاسم لصاحب المطعم بعد التجربة:** «هل ستدفع شهرياً مقابل هذا؟»
جوابه أهم من كل المؤشرات التقنية.

---

## القسم 6: ما ينقص التطبيق فعلياً لتجربة تجارية (كن صريحاً مع نفسك)

هذه أمور **غير موجودة** الآن، واكتشافها بعد التجربة أرخص من اكتشافها بعد التعاقد:

1. **لا نظام طلبات حقيقي** — زر "Add to Order" يعمل في ذاكرة المتصفح فقط
   ولا يصل إلى المطبخ ولا إلى الكاشير.
2. **لا إحصاءات ظاهرة لصاحب المطعم** — البيانات تُجمع (`views`, `ar_views`)
   لكن لا توجد شاشة تعرضها له. وهذه أول ما سيطلبه.
3. **الترجمة جزئية** — واجهة AR إنجليزية إلى حد كبير، بينما زبائنك عرب.
4. **لا فاتورة/اشتراكات** — لا آلية دفع للمطاعم.

اقتراحي: افعل التجربة الآن بهذه النواقص، ودع صاحب المطعم يخبرك ما يهمّه فعلاً
قبل أن تبني الأربعة.

---

## القسم 7: توسيع النظام لأربعة مطاعم (المرحلة 2)

المطلوب برمجياً قبل توزيع الحسابات:

1. **شاشة دخول في `/admin`** تستخدم `POST /api/auth/login` الموجود مسبقاً،
   وتحمل `Authorization: Bearer <token>` بدل مفتاح الإدارة المشترك.
2. **عزل البيانات (الأهم):** إضافة تحقق في `requireAdmin` بأن
   `req.auth.restaurantId` يطابق المطعم المستهدف في الطلب، ورفض ما عداه
   بـ 403 — إلا لدور `admin` العام (أنت).
3. **إضافة `owner_id` أو تفعيل سياسات RLS المرتبطة بالمالك** على
   `restaurants`/`menus`/`dishes`، لتكون الحماية على مستوى قاعدة البيانات
   لا الكود فقط (طبقتا دفاع).
4. **دعوة المستخدمين:** `POST /api/auth/signup` محميّ بمفتاح الإدارة الآن —
   أنت تُنشئ الحساب وتحدد `role=owner` و `restaurant_id`، ثم يستلم صاحب
   المطعم رابط تعيين كلمة المرور.

**التقدير الزمني الواقعي: يوم إلى يومين عمل.** أخبرني وأنفّذها.

**ملاحظة عن النطاقات:** إن أراد مطعم نطاقاً خاصاً (`menu.firas.com`) فهذا
لا يحتاج نسخة مستقلة — يكفي توجيه النطاق إلى نفس الاستضافة وربطه بالـ slug.

---

## القسم 8: ترتيب التنفيذ المقترح

```
اليوم        → أرسل قائمة القسم 1 لمطعم فراس
+2 أيام      → جلسة تصوير الموديلات (القسم 2، الطريقة أ)
+3 أيام      → إدخال البيانات (القسم 3) واختبار الهاتف (القسم 4)
+4 أيام      → التجربة الميدانية (القسم 5)
+5 أيام      → قرار: هل يستحق التوسّع؟ وماذا نُصلح أولاً؟
بعد القرار   → المرحلة 2 (القسم 7) ثم المطاعم الثلاثة
```

**لا تُضِف المطاعم الثلاثة قبل نجاح فراس.** أربعة مطاعم بمشكلة واحدة
= أربع مكالمات غاضبة بدل واحدة.
