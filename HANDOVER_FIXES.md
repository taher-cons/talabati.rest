# تقرير إصلاح صفحة المستخدم — جاهزية التسليم

**التاريخ:** 2026-07-31 · **النطاق:** https://talabati.rest · **المطعم التجريبي:** Firasse Food (`firasse_food`)

---

## 1. المشكلة كما ظهرت

صفحة المستخدم (`/menu`) كانت تُحمّل ثم تظهر فارغة أو مع رسالة خطأ، وكان الـ Console يُظهر
`HTTP 500` على نداء المطعم. الأطباق التي أُضيفت من لوحة التحكم لم تظهر أبداً.

بعد الفحص تبيّن أن السبب **ليس واحداً** بل خمسة أسباب متراكبة، ثلاثة منها في الكود واثنان
في البيانات.

---

## 2. أسباب في الكود (أُصلحت ونُشرت)

### 2.1 نداء API على مسار خاطئ — كان يُرجع 500
`public/menu/main.js` كان ينادي `/api/menu/restaurant/:id` ويمرّر له **الـ slug** بدلاً من
المعرّف (uuid). Postgres يرفض تحويل `"firasse_food"` إلى uuid فيرتفع الخطأ إلى 500.

**الإصلاح:** استخدام المسار الصحيح `/api/menu/restaurant/slug/:slug`، ثم استخدام الـ uuid
العائد منه لجلب القائمة.

### 2.2 استخراج الـ slug من الرابط
عند فتح `/menu` بدون اسم مطعم، كان الكود يأخذ آخر مقطع من المسار — أي الكلمة `"menu"`
نفسها — ويستعلم عن مطعم بهذا الاسم.

**الإصلاح:** دالة `resolveRestaurantSlug()` تعتمد الترتيب التالي:
1. `/menu/<slug>` من المسار
2. `?r=<slug>` أو `?restaurant=<slug>` من الاستعلام
3. المطعم التجريبي `firasse_food` كقيمة احتياطية

### 2.3 هشاشة في العرض
- **تحذيرات GSAP:** كانت الحركات تُطبَّق على `.menu-category` قبل بناء الأقسام، فيمتلئ
  الـ Console بتحذيرات. الآن `animateIfPresent()` تتحقق من وجود العنصر أولاً.
- **صور مكسورة:** الطبق الذي لا يملك صورة كان يُعرض بأيقونة الصورة المعطوبة. الآن هناك
  صورة بديلة مدمجة (SVG)، وشعار المطعم يرجع لأيقونة التطبيق عند غيابه.
- **العملة:** كانت `USD` افتراضياً وبتنسيق إنجليزي. الآن `DZD` بتنسيق `ar-DZ`، مع حماية
  من رموز عملات غير صحيحة حتى لا تتعطل الصفحة.
- **`ReferenceError` عند الإضافة للطلب:** `addToOrder` كان يعتمد على `window.event`
  الضمني (غير موجود في وضع الوحدات النمطية)، فكان زر "أضف" في نافذة الطبق يرمي خطأ.
  الآن يُمرّر الحدث صراحةً.

### 2.4 التوجيه في `firebase.json`
النمط `/menu/**` لا يطابق `/menu` بدون شرطة مائلة، فكانت الصفحة تفوت رأس الأمان الخاص
بها. أُضيفت مسارات صريحة لـ `/menu` و `/ar` و `/admin`، وصار نمط الـ CSP هو `/menu{,/**}`.

### 2.5 الكاش كان يُخفي كل إصلاح لمدة ساعة ⚠️
بعد أول نشر ناجح، ظلّت الصفحة تُظهر **نفس الأخطاء القديمة** في المتصفح:
`api/menu/restaurant/menu → 404` و `Restaurant not found`. تبيّن أن الخادم كان يخدم النسخة
الصحيحة فعلاً، لكن **المتصفح كان يشغّل نسخة `main.js` القديمة من الكاش**.

السبب: ملفات JS/CSS لا تحمل بصمة محتوى في أسمائها (`main.js` وليس `main.a1b2c3.js`)،
ومع ذلك كانت سياسة الكاش `max-age=3600`. أي أن **كل إصلاح يُنشر يبقى غير مرئي للعملاء
حتى ساعة كاملة** — وهذا غير مقبول في مرحلة تجريبية.

**الإصلاح المزدوج:**
- سياسة الكاش لملفات JS/CSS صارت `no-cache, must-revalidate` — المتصفح يتحقق من الخادم
  في كل مرة ويستقبل `304` إن لم يتغيّر شيء (تكلفة مهملة، وضمان كامل).
- أُضيف `?v=20260731` لروابط JS/CSS في صفحات `/menu` و `/ar` و `/admin` لتجاوز النسخ
  المخزّنة مسبقاً في أجهزة من فتح الموقع قبل الإصلاح. صفحات HTML أصلاً `no-store`
  فتصل التحديثات فوراً.

> **عند أي تعديل مستقبلي على JS/CSS:** إن أردت ضماناً مطلقاً، ارفع رقم `?v=` في
> ملف `index.html` للصفحة المعنية. سياسة `no-cache` الحالية تكفي في العادة.

---

## 3. أسباب في البيانات (أُصلحت في قاعدة البيانات)

هذان السببان هما التفسير الحقيقي لغياب الأطباق:

### 3.1 خمس قوائم متطابقة كلها مُفعّلة
كان للمطعم **5 قوائم** جميعها باسم `Main Menu` وجميعها `is_active = true`. صفحة المستخدم
تعرض **الأحدث فقط**، بينما لوحة التحكم تُظهر القوائم الخمس بنفس الاسم — فلا يستطيع
المستخدم معرفة أيّها المعروضة فعلاً.

**الإصلاح:** أُبقيت قائمة واحدة مُفعّلة وعُطّلت الأربع الأخرى.

### 3.2 جدول الأطباق كان فارغاً تماماً
لم يكن هناك **أي طبق** في قاعدة البيانات للمطعم. أي أن محاولات الإضافة السابقة لم تُحفظ.

**الإصلاح:** زُرعت قائمة جزائرية واقعية من 9 أطباق بأسعار بالدينار وأسماء عربية،
موزّعة على الفئات الأربع الموجودة:

| الفئة | الأطباق |
|---|---|
| Appetizers | شوربة فريك (350) · بوراك باللحم (450) |
| Main Courses | كسكس ملكي (1800) · طاجين زيتون (1500) · مشوي الغنم (2400) |
| Desserts | بقلاوة (400) · قلب اللوز (350) |
| Drinks | شاي بالنعناع (150) · عصير برتقال طازج (250) |

---

## 3.3 المحتوى الحقيقي للمطعم (صور + أوصاف)

استُبدلت الأطباق التجريبية بمحتوى Firasse Food الفعلي من `obs_58.txt` وصور
`public/firasse_resto/`:

| الصورة | الطبق | الفئة | السعر |
|---|---|---|---|
| `c1.png` | حبار محشو مشوي على طريقة المتوسط | المأكولات البحرية | 1800 د.ج |
| `c2.webp` | بيتزا نابولي الإيطالية الفاخرة | البيتزا | 1200 د.ج |
| `c3.webp` | بيتزا البيبروني الفاخرة بالجبن الذائب | البيتزا | 1400 د.ج |
| `c4.webp` | كروكيت الدجاج المقرمش محشو بالجبن الذائب | المقبلات | 900 د.ج |

كل طبق يحمل الاسم والوصف بالعربية والإنجليزية، السعر بالدينار، القيم الغذائية،
مسبّبات الحساسية، والوسوم. الفئات الأربع القديمة استُبدلت بثلاث فئات مطابقة للمحتوى
(المقبلات · البيتزا · المأكولات البحرية) بأسماء عربية وأيقونات.

### ⚠️ جدول الأطباق لا يملك عموداً للصورة
عند ربط الصور تبيّن أن جدول `dishes` **لا يحتوي أي عمود للصورة** — لا `image` ولا
`image_url`. الأعمدة الموجودة: `name, name_ar, description, description_ar, price,
currency, category, model3d, ar_config, nutrition, is_available, is_featured,
display_order`.

الحقل الوحيد الذي يمرّره الـ API حالياً هو `model3d.thumbnail`، وصفحة المستخدم تقرأه
أولاً: `dish.model3D?.thumbnail || dish.image || dish.imageUrl || placeholder`.
لذلك خُزّنت صور الأطباق فيه — وهو المسار المدعوم بالكامل من الـ API واللوحة.

**هذا هو التفسير الجذري لغياب الصور:** أي صورة تُرفع من لوحة التحكم لا تجد عموداً
تُحفظ فيه إلا هذا الحقل.

**التوصية بعد التسليم:** إضافة عمود `image_url TEXT` إلى `dishes`، ثم إضافته إلى
`transformDishInput` و`transformDish` في `functions/src/supabase.js`، ونقل القيم الحالية
من `model3d.thumbnail` إليه — ليبقى `thumbnail` مخصصاً لمعاينة المجسم ثلاثي الأبعاد.

---

## 3.4 لوحة التحكم كانت تُفسد كل طبق يُضاف منها ⚠️

بعد ساعات من النشر، ظهر في القائمة الحيوية طبقان جديدان أضافهما صاحب المشروع من
لوحة التحكم: **"بيتزا بيبروني فاخرة" — مرتين، بسعر 1200 USD، بلا صورة، وبفئة
`special`**.

هذا خبر جيد وسيّئ في الوقت نفسه: **مسار الحفظ في اللوحة يعمل فعلاً** (كان مشكوكاً فيه
في القسم 6)، لكن اللوحة نفسها كانت تُنتج بيانات معطوبة. أربعة أخطاء، كلها أُصلحت:

### 1. الدينار الجزائري لم يكن موجوداً في قائمة العملات
خيارات العملة كانت `USD, EUR, GBP, SAR, AED` — **بلا DZD**. لم يكن أمام صاحب المطعم
الجزائري أي خيار سوى حفظ أسعاره بالدولار. أُضيف `DZD` وصار **الافتراضي** في نموذج الطبق
وفي الإعدادات العامة، وتغيّر الافتراضي في الكود من `'USD'` إلى `'DZD'`.

### 2. قائمة الفئات كانت ثابتة ولا تطابق فئات المطعم
كانت الخيارات مكتوبة في الـ HTML: `appetizer / main / dessert / drink / special`، بينما
فئات المطعم الحقيقية هي `Starters / Pizzas / Seafood`. لذلك حُفظت البيتزا في فئة
`special` **التي لا وجود لها في القائمة**، فلا تظهر للزبون تحت أي قسم.

الحل: دالة `populateDishCategories()` تبني الخيارات من فئات القائمة المختارة فعلاً
(المصدر: `menu.categories` كما يرجعها الـ API)، وتتحدث تلقائياً عند تغيير القائمة، وتحفظ
فئة الطبق الحالية حتى لو لم تعد موجودة (تُعرض بوسم `not in menu`). أُضيف كذلك تحقق يمنع
الحفظ بلا فئة.

### 3. أي تعديل لطبق كان يمسح صورته وموديله ثلاثي الأبعاد
`handleDishSubmit` يعيد بناء `model3D` و`arConfig` من حقول النموذج فقط. وبما أن الصورة
تُخزَّن في `model3d.thumbnail` ولا يوجد لها حقل نصي في النموذج، فإن أي تعديل لاحق —
تغيير السعر مثلاً — كان **يُرسل `model3D` بلا `thumbnail` ولا `url`، فيمسح صورة الطبق
وملف الـ GLB معاً**. فقدان بيانات صامت.
الحل: دمج القيم الموجودة قبل الإرسال (`{ ...existing.model3D, ...formData.model3D }`).
وإذا فشل رفع الملفات، تتوقف العملية بدل حفظ طبق بملفات ناقصة.

### 4. الضغط مرتين على "Save" كان يُنشئ طبقين
لا شيء كان يمنع الإرسال المزدوج — وهذا هو سبب النسختين. أُضيف قفل `state.savingDish`
مع تعطيل الزر وتغيير نصه إلى `Saving…` حتى انتهاء الطلب.

### تنظيف الأثر
- حُذفت النسخة المكرّرة بـ `dedupe-dishes.mjs`.
- صُحّح الطبق الباقي إلى `1200 DZD` وفئة `Pizzas` بـ `fix-dish.mjs` بدل حذف عمل صاحب
  المشروع. لا تزال بلا صورة، ولأنها تكرار لـ "بيتزا البيبروني الفاخرة" الحقيقية فيمكن
  حذفها من اللوحة بضغطة إن أردت.

---

## 4. أدوات جديدة للتشغيل

| الأداة | الغرض |
|---|---|
| `node scripts/diagnose-menu.mjs <slug>` | يطبع كل قوائم المطعم وعدد أطباق كل قائمة ويحدّد القائمة المعروضة للعملاء، ويشخّص سبب أي فراغ. أضف `--fix` لنقل الأطباق المعلّقة إلى القائمة المعروضة. |
| `node scripts/seed-pilot-menu.mjs <slug>` | يوحّد القوائم المُفعّلة ويزرع قائمة تجريبية. يعمل كتجربة جافة افتراضياً، و`--apply` للتنفيذ. |
| `node scripts/seed-firasse-real.mjs` | يزرع أطباق Firasse Food الحقيقية بصورها وأوصافها وفئاتها. `--apply` للتنفيذ. |
| `node scripts/dedupe-dishes.mjs <slug>` | يكشف الأطباق المكرّرة في القائمة الحيوية ويحذفها، مع **الإبقاء على النسخة الأغنى** (صورة + وصف) لا الأحدث. `--apply` للتنفيذ. |
| `node scripts/fix-dish.mjs <dishId>` | يفحص طبقاً واحداً ويصلح عملته أو فئته أو صورته: `--currency DZD --category Pizzas --photo /path.webp --apply`. |
| `node scripts/inspect-columns.mjs` | يطبع أعمدة الجداول الفعلية — مفيد قبل أي زرع بيانات. |

---

## 5. حالة التحقق بعد النشر

| الفحص | النتيجة |
|---|---|
| `GET /api/menu/restaurant/slug/firasse_food` | **200** — بيانات المطعم صحيحة |
| `GET /api/menu/restaurant/<uuid>` | **200** — 3 فئات و**4 أطباق حقيقية بصورها** |
| `GET /api/menu/<menuId>` (نداء صفحة AR) | **200** |
| `GET /menu` | **200** |
| `GET /menu/firasse_food` | **200** |
| `GET /ar/<menuId>` | **200** |
| `GET /api/menu/restaurant/<uuid>` بعد إصلاح اللوحة | **200** — 5 أطباق، **كلها بالدينار وفي فئات صحيحة** |
| صور الأطباق `c1..c4` | **200** — الأربع تُخدم فعلاً |
| فحص صياغة `public/admin/main.js` | سليم |
| رأس الكاش لملفات JS | `no-cache, must-revalidate` |
| فحص الصياغة لكل الملفات المعدّلة | سليم |
| النشر (Functions + Hosting) | ناجح |

---

## 6. ما يجب الانتباه له قبل التسليم

1. **تأكّد من مطابقة كل صورة لطبقها.** رُبطت الصور بترتيب ظهور الأطباق في `obs_58.txt`
   (`c1`→الحبار، `c2`→نابولي، `c3`→البيبروني، `c4`→الكروكيت). راجعها بصرياً؛ وإن اختلف
   ترتيب أي صورة، عدّل حقل `image` في `scripts/seed-firasse-real.mjs` وأعد تشغيله.
2. **لا توجد موديلات ثلاثية الأبعاد بعد.** الصور ثنائية الأبعاد جاهزة، لكن تجربة الواقع
   المعزز تحتاج رفع ملف `GLB` لكل طبق.
3. **بيانات المطعم في التذييل وهمية** (`123 Culinary Street, Food City`) — حدّث العنوان
   والهاتف والبريد من لوحة التحكم قبل العرض.
4. **الصفحة تفتح بالإنجليزية افتراضياً** مع أن الوصف العربي أغنى وجمهورك عربي. زر اللغة
   في الأعلى يبدّل فوراً. إن أردت العربية كافتراضي فهو تغيير صغير في `public/menu/main.js`
   يُفضّل اختباره بصرياً أولاً (تخطيط RTL).
5. **لا تُنشئ قوائم متعددة.** أنشئ قائمة واحدة واعمل عليها؛ كل قائمة إضافية مُفعّلة تُخفي
   ما قبلها عن العملاء. شغّل `diagnose-menu.mjs` عند أي شك.
6. **عند إضافة طبق من اللوحة، ارفع صورته في حقل `Thumbnail`** داخل قسم
   *3D Model Configuration* — فلا يوجد عمود صورة مستقل في الجدول (راجع القسم 3.3).
   الفئة تُعرض الآن من فئات قائمتك الحقيقية، والعملة `DZD` افتراضياً.
7. **الإضافة من اللوحة تعمل وتم التحقق منها عملياً** (راجع القسم 3.4). للتأكد بعد أي
   إضافة: `node scripts/diagnose-menu.mjs firasse_food`، ثم `node scripts/dedupe-dishes.mjs
   firasse_food` إن شككت في وجود تكرار.
8. **الطبق التجريبي "بيتزا بيبروني فاخرة" لا يزال في القائمة** بلا صورة وهو تكرار مضموني
   لـ "بيتزا البيبروني الفاخرة". احذفه من اللوحة أو ارفع له صورة قبل العرض.
9. **زر إتمام الطلب** لا يزال يُظهر "قريباً" — الطلب يُبنى في الواجهة فقط ولا يُرسل للمطبخ.

---

## 7. الملفات المعدّلة

```
public/menu/main.js            المسار الصحيح + استخراج الـ slug + الصور البديلة + العملة + إصلاح الحدث + GSAP
firebase.json                  توجيه صريح لـ /menu و /ar و /admin + نمط CSP
functions/src/routes/menu.js   تحويل بيانات المسارات العامة
scripts/diagnose-menu.mjs      جديد — تشخيص بيانات القوائم
scripts/seed-pilot-menu.mjs    جديد — توحيد القوائم وزرع البيانات
scripts/seed-firasse-real.mjs  جديد — زرع أطباق المطعم الحقيقية بصورها وأوصافها
scripts/inspect-columns.mjs    جديد — طباعة أعمدة الجداول الفعلية
scripts/dedupe-dishes.mjs      جديد — إزالة الأطباق المكرّرة مع الإبقاء على الأغنى
scripts/fix-dish.mjs           جديد — إصلاح عملة/فئة/صورة طبق واحد
public/admin/index.html        إضافة DZD كعملة افتراضية + قائمة فئات ديناميكية
public/admin/main.js           بناء الفئات من القائمة + حفظ الصورة والموديل عند التعديل + منع الإرسال المزدوج
public/firasse_resto/          صور الأطباق الأربع (تُخدم من الاستضافة)
```

---

## 8. جلسة تفعيل الواقع المعزز (obs_57)

الوعد الأساسي للتطبيق — «شاهد طبقك ثلاثي الأبعاد» — لم يكن يعمل فعلياً. ما تم:

### 8.1 لا يوجد أي موديل مرتبط بأي طبق
كل الأطباق كانت `model_3d_url = NULL`، فكل زر «شاهد بالواقع المعزز»
يؤدي إلى كاميرا فارغة. تم ربط موديل البيتزا الحقيقي
(`/firasse_resto/piza3d.glb`, ‏966 KB) بطبق البيتزا.

### 8.2 زر AR كان يظهر على كل الأطباق
الآن يظهر فقط على الأطباق التي تملك موديلاً فعلاً، مع شارة **«🧊 ثلاثي الأبعاد»**
على صورة الطبق. الأطباق الأخرى تعرض زر «التفاصيل» فقط — لا وعود كاذبة.

### 8.3 خطأ جوهري في المقياس (الأخطر)
كان الكود يضرب كل موديل في ثابت أعمى `modelScale = 0.01`. موديل البيتزا
عرضه 0.92 وحدة ⇒ الناتج **بيتزا بعرض 9 ملّيمتر**: غير مرئية عملياً.
والحدّ القديم للتكبير `clamp(0.001, 0.1)` كان يعيد أي حجم واقعي إلى نقطة صغيرة.

الحل: قياس الصندوق المحيط للموديل بعد التحميل وملاءمته تلقائياً إلى حجم طبق
حقيقي (`targetDishSize = 0.28 m`)، وجعل قيمة `scale` في قاعدة البيانات مجرد
تعديل نسبي (1 = الحجم الطبيعي). أي موديل يرفعه المطعم لاحقاً — بأي وحدات —
سيظهر بحجم صحيح بلا حسابات يدوية. وحدود التكبير أصبحت نسبية (0.4×–3×).

### 8.4 تحسين الأداء على شبكة الهاتف
صور الأطباق كانت PNG بحجم 1.5–2.4 MB للصورة الواحدة (‏7.4 MB للقائمة).
تم تحويلها إلى WebP: **‏7.4 MB ← 0.55 MB (‏−93%)** مع الإبقاء على الجودة.

```
public/ar/main.js              ملاءمة تلقائية للمقياس + حدود تكبير نسبية + تصحيح مقياس الماركر
public/menu/main.js            زر AR شرطي + شارة «ثلاثي الأبعاد»
public/menu/styles.css         تنسيق شارة AR (مع احترام prefers-reduced-motion)
scripts/optimize-images.mjs    جديد — تحويل صور الأطباق إلى WebP
scripts/link-pizza-model.mjs   جديد — ربط موديل GLB بطبق
```
