# تقرير إصلاح تجربة الزبون (obs_57)

الحالة: **منشور ومُختبَر على https://talabati.rest** — `node scripts/verify-view.mjs` يمرّ بكل الفحوص.

---

## 1) سبب المشكلة الأصلية: التعليق على «جاري البحث عن سطح»

صفحة `/ar` كانت تحمّل **AR.js (تتبّع العلامات) و WebXR (`ar-hit-test`) في مشهد A-Frame واحد**.

- WebXR لا تبدأ جلسة إلا بلمسة صريحة من المستخدم، و AR.js لا تبدأها أبداً.
- النتيجة: الحدث `hit-test-result` **لا يُطلق مطلقاً**، فتبقى الرسالة معلّقة إلى الأبد.
- على iPhone الأمر أسوأ: Safari لا يدعم WebXR إطلاقاً، أي أن الشاشة **لم تكن لتعمل أبداً**.

هذا خلل معماري لا يُصلح بتعديل رسالة أو مؤقت؛ لذلك استُبدل المسار.

## 2) ما نُفّذ

| # | التغيير | الأثر على الزبون |
|---|---------|------------------|
| 1 | صفحة عرض جديدة `/view` مبنية على `<model-viewer>` من Google | الطبق **يظهر دائماً** بالأبعاد الثلاثة ويُدوَّر بالإصبع، ثم يُفتح في الواقع المعزز عبر Scene Viewer (أندرويد) أو Quick Look (آيفون) |
| 2 | نقل `/ar` القديمة إلى `archive/ar-legacy-arjs/` + تحويل `/ar` و `/ar/**` إلى العارض الجديد | أي رمز QR أو رابط مطبوع سابقاً **لم يعد يعلّق** |
| 3 | زر «الواقع المعزز» في الرأس كان يفتح كاميرا بلا طبق؛ الآن يفتح أول طبق له مجسّم فعلي، وإن لم يوجد يظهر إشعار واضح | لا كاميرا فارغة بعد الآن |
| 4 | شارة «مجسّم ثلاثي الأبعاد 🧊» تظهر **فقط** على الأطباق التي تملك ملف GLB | الزبون يعرف أين يضغط، ولا يُوعد بما لا يوجد |
| 5 | إصلاح الكاروسيل: كان يتحرك بمقدار ثابت 296px ويسمح بالوصول لآخر بطاقة | هذا كان سبب **«انزلاق المحتوى جانباً ونصف الشاشة فارغ»** على الهاتف |
| 6 | السلة تُحفظ في `localStorage` بمفتاح مشترك بين `/menu` و `/view` | الطلب **لا يُفقد** عند الذهاب لمعاينة الطبق والعودة |
| 7 | تصحيح نص المحاكاة في الصفحة الرئيسية («جاري البحث عن سطح» → وصف ما يحدث فعلاً) | التوقّع يطابق الواقع |
| 8 | `scripts/verify-view.mjs`: يفحص الصفحة + الـAPI + **تنزيل ملف GLB والتحقق من توقيعه** | أي انقطاع في هذه السلسلة يُكتشف قبل الزبون |

## 2-ب) لماذا ظهر المجسّم **أبيض** بلا صلصة ولا بيبروني

فحصت ملف GLB نفسه (`scripts/inspect-glb.mjs`) بدل التخمين، والنتيجة:

```
materials: 1   textures: 2   images: 2 (مضمّنة JPEG)
material[0] baseColorTexture = yes
primitive[0] attributes: POSITION, TEXCOORD_0, NORMAL
bounding box: X=0.920  Y=0.408  Z=0.842
```

**الملف سليم تماماً** — النسيج مضمّن وإحداثيات UV موجودة. الخطأ كان في صفحتي:

1. **زاوية الكاميرا:** الافتراضي في `model-viewer` هو ميل 75° أي نظرة **شبه أفقية على حافة العجينة**، والنسيج (الصلصة والبيبروني) مطبوع على **السطح العلوي** فقط. لذلك رأيت قرصاً أبيض سميكاً. ضبطتها على 50° من الأعلى، مع `max-camera-orbit=85°` حتى لا يدور الزبون إلى **أسفل** الطبق حيث لا نسيج، و`field-of-view=32deg` ليختفي التمدد والقص الذي رأيته في الصورة.
2. **المقياس:** المجسّم مُصمَّم بعرض **92 سم وسماكة 41 سم** — بيتزا بحجم طاولة. مع `ar-scale="fixed"` كانت ستوضع على طاولة الزبون بهذا الحجم العملاق. أضفت تطبيعاً تلقائياً: أي مجسّم خارج نطاق 10–45 سم يُصغَّر إلى 34 سم (حجم طبق واقعي). هذا يحمي المنصة من **أي** ملف يرفعه أي مطعم مستقبلاً، لا من هذا الملف وحده.

## 2-ج) الفراغ الكبير في قسم «Our Specialties»

كان قسم الأطباق المميّزة يظهر **فارغاً تماماً**، وأحياناً ببطاقة واحدة مقصوصة على الحافة. السبب تعارض مباشر داخل الكود:

- `styles.css` يجعل `.carousel-track` **حاوية تمرير أصلية** (`overflow-x: auto` + `scroll-snap-type: x mandatory`).
- وفي الوقت نفسه كان جافاسكربت يحرّكها بـ`transform: translateX(...)`.

الميكانيزمان يتصارعان، و`.featured-carousel { overflow: hidden }` يقصّ الناتج. وفي **RTL** تصبح فروق `offsetLeft` سالبة، فتنقلب إشارة الخطوة ويُدفع كل المحتوى خارج الشاشة — وهذا سبب الفراغ الكامل.

الحل: حذف كل الحسابات اليدوية والاعتماد على التمرير الأصلي (`scrollBy` + scroll-snap). المتصفح يتولى RTL والسحب باللمس والزخم والحدود بشكل صحيح، فاختفى أيضاً كود اللمس اليدوي ومعالج `resize`. أضفت فحصاً في `verify-view.mjs` يمنع رجوع `translateX` إلى الكاروسيل مستقبلاً.

## 3) نتيجة الفحص الفعلي (لا افتراضات)

```
PASS  /view returns 200
PASS  /view HTML contains <model-viewer>
PASS  camera framed from above (50°)
PASS  orbit clamped so the customer cannot spin under the plate
PASS  narrow field of view (no fish-eye distortion)
PASS  oversized models are rescaled to a realistic plate size before AR
PASS  carousel uses native scrolling (no translateX transform)
PASS  carousel arrows scroll by one real card width
PASS  restaurant "Firasse Food" resolved
PASS  1 dish(es) have a 3D model
PASS  dish API returns a model URL for "بيتزا بيبروني فاخرة"
PASS  model downloads: 966 KB, model/gltf-binary
PASS  /ar -> 200 يخدم العارض الجديد
```

## 4) ما لم يُحل بعد — يجب أن تعرفه قبل التجربة الميدانية

1. **طبق واحد من خمسة يملك مجسّماً.** التجربة لن تُبهر الزبون بأربعة أطباق بلا مجسّم. الحل: رفع GLB لبقية الأطباق من لوحة التحكم.
2. **الواقع المعزز «على الطاولة» لا يعمل على آيفون** لأن Quick Look يحتاج ملف `USDZ` وليس GLB. العارض يقول ذلك للمستخدم بصراحة بدل تركه يبحث عن زر. لدعم آيفون كاملاً: توليد USDZ لكل مجسّم.
3. **أرقام وادعاءات غير مدعومة في الصفحة الرئيسية:** «500+ نموذج»، «50+ مطعم نشط»، «95% معدل نجاح جلسة AR»، وميزة «علامات مخصصة (Markers)» التي أُزيلت فعلياً مع صفحة AR.js. أنصح بحذفها أو تصحيحها قبل عرض المنصة على مطاعم.
4. حجم المجسّم الحالي 966 KB مقبول؛ السكربت ينبّه تلقائياً إذا تجاوز أي مجسّم 5 MB (ثقيل على بيانات الهاتف داخل مطعم).
5. **جودة المجسّم ذاته محدودة:** سماكة 41 سم مقابل قطر 92 سم تعني نموذجاً مُسحاً ضوئياً (trimesh) غير محسّن — بيتزا منتفخة أكثر من الواقع. التطبيع يصحّح الحجم لا الشكل. لتجربة تُبهر الزبون: نموذج مُنمذَج يدوياً أو مسح أنظف، والأفضل توليد **USDZ** معه ليعمل على آيفون.

## 6) أدوات التشخيص التي أُضيفت

- `scripts/verify-view.mjs` — يفحص الصفحة، إعدادات الكاميرا، الـAPI، وينزّل GLB ويتحقق من توقيعه.
- `scripts/inspect-glb.mjs` — يفتح أي مجسّم ويطبع المواد والنسيج وإحداثيات UV والحجم الحقيقي بالمتر، مع تحذير إذا كان المقياس غير واقعي. **استخدمه على كل مجسّم قبل رفعه للمطعم.**

## 5) كيف تعيد التحقق في أي وقت

```bash
node scripts/verify-view.mjs https://talabati.rest firasse_food
```
