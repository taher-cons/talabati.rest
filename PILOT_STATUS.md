# حالة تجربة Firasse Food (Beta) — ملف تتبّع للمتابعة بين الجلسات

**آخر تحديث:** جلسة إصلاح ما قبل إرسال الرابط لصاحب المطعم (بعد تقرير AR_FIX_REPORT.md).
**النطاق المباشر:** https://talabati.rest/menu/firasse_food

الهدف من هذا الملف: أي جلسة قادمة (أنا أو أي مطوّر آخر) تعرف **بالضبط** ما تم إنجازه،
لماذا، وما الذي يجب إكماله بعده — بدون إعادة تشخيص ما شُخِّص فعلاً.

---

## ✅ ما تم في هذه الجلسة (بالترتيب)

### 1. رفع المشروع على GitHub
- لم يكن هناك مستودع Git من الأساس. أُنشئ، ورُبط بـ `https://github.com/taher-cons/talabati.rest`.
- `.gitignore` حُدِّث لاستثناء `.env` والأسرار وملفات التصحيح المؤقتة (`obs_*.txt` إلخ).
- تم التحقق أن `.env` **لم يُرفع أبداً** (فقط `.env.example`).
- Commits: `3f1f70e` (أولي) → `c4bb6b2` → (هذه الجلسة، انظر أسفل).

### 2. حذف طبق الاختبار من القائمة الحية
طبق **"اختبار الصورة - البطة المطاطية"** (`dff21722-3b7b-4e53-aa75-53c97cc40ebe`) حُذف
نهائياً من جدول `dishes`. لن يظهر بعد الآن لصاحب المطعم.
السكربت: `scripts/finalize-pilot-menu.mjs` (يمكن حذفه بعد التأكد أنه لم يعد يُستخدم كأداة متكررة، أو الاحتفاظ به كمرجع).

### 3. تشخيص وإصلاح جذر مشكلة "طبق الحبار يظهر عمودياً"
- **لم نُخمّن** — فتحنا الملف القديم `c_1.glb` بسكربت `scripts/inspect-glb-local.mjs` وقرأنا
  الصندوق المحيط الحقيقي: `X=0.956m  Y(الارتفاع)=0.739m  Z(العمق)=0.215m`.
  هذا فعلياً "شريحة رفيعة طويلة قائمة عمودياً" — نفس وصف المستخدم بالضبط، وليست مشكلة
  زاوية كاميرا يمكن حلها بـ`rotation` فقط.
- الملف الجديد الذي أضافه المستخدم (`grilled_stuffed_calamari_with_roasted_tomatoes_3d.glb`,
  من THREE.GLTFExporter) مُصمَّم أصلاً مسطحاً (`Y=0.12m` ارتفاع مقابل `X=Z=4.4m` قاعدة) —
  التوجيه سليم من الأصل، **لكن الحجم ضخم جداً (4.4 متر!)**.

### 4. اكتشاف نشر ملف تالف على Firebase Hosting (خلل جانبي غير متوقع)
عند فحص الملف **على الإنتاج فعلياً** (لا افتراضاً) بعد ربطه بالطبق، تبيّن أن Firebase كان
يخدم نسخة **31 KB فقط** بدل 2.3 MB الحقيقية — ملف تالف من نشر سابق. أُعيد نشر الـhosting
(`firebase deploy --only hosting`) وتحقّقنا بتحميل الملف وفحص توقيعه أن النسخة الصحيحة
أصبحت تُخدَّم. **هذا تنبيه مهم لأي جلسة قادمة: تحقق دائماً من الملف الفعلي على
الإنتاج بعد أي نشر لا تفترض أن ما هو محلي = ما هو منشور.**

### 5. مشكلة "يظهر كنقطة" بعد الربط الأول — السبب والحل النهائي
الربط الأول اعتمد على تصغير الحجم **من JavaScript فقط** (`normalizeScale()` في
`public/view/main.js` تضبط خاصية `scale` على عنصر `<model-viewer>` بعد التحميل).
هذا **لا يكفي** عندما يكون معامل التصغير كبيراً جداً (0.077× هنا، أي 13 ضعف تقريباً):

- تأطير الكاميرا الافتراضي في `model-viewer` (`camera-orbit` بالنسبة المئوية) يُحسَب على
  أساس أبعاد الموديل **الأصلية الضخمة** (4.4م)، ولا يُعاد حسابه تلقائياً بعد تصغير الموديل
  عبر خاصية `scale` من الخارج — فتبقى الكاميرا "متباعدة" كما لو أن الموديل لا يزال 4.4م،
  فيظهر الموديل المصغَّر كنقطة صغيرة.
- الأخطر: تصغير الحجم عبر `scale` في الـJS **لا ينتقل لتجربة AR الحقيقية** (Scene Viewer /
  Quick Look) لأن هذين يحمّلان ملف الـGLB الخام مباشرة ولا يعرفان بخاصية `scale` التي
  ضبطناها فقط على عنصر `<model-viewer>` في المتصفح.

**الحل المطبَّق (نفس فلسفة إصلاح البيتزا الأصلي في `fix-glb-model.mjs`):** أُنشئ ملف جديد
`public/firasse_resto/calamari_fixed.glb` بتشغيل:
```
node scripts/fix-glb-model.mjs public/firasse_resto/grilled_stuffed_calamari_with_roasted_tomatoes_3d.glb \
  public/firasse_resto/calamari_fixed.glb --diameter 0.32
```
هذا يخبز عامل التصغير (0.0727×) **داخل مصفوفة تحويل العنصر الجذري في الملف نفسه**، فتصبح
الأبعاد الحقيقية داخل الملف: قاعدة 0.32م × ارتفاع 0.9 سم — منطقي تماماً لطبق حبار. هذا يحل
المشكلة في المعاينة العادية **وفي AR الحقيقي بنفس الوقت**، لأنه لا يعتمد على أي حساب JS
وقت التشغيل. ثم ضبطنا `model3d.scale = {1,1,1}` في قاعدة البيانات (لا حاجة لأي مضاعف JS بعد
الآن)، ونُشر الملف عبر `firebase deploy --only hosting`، وتحقّقنا من تنزيله من الإنتاج.

**⚠️ درس عام للمستقبل:** أي ملف GLB يحتاج تصغيراً/تكبيراً بمعامل كبير (أكبر من ~3× في أي
اتجاه) **يجب** أن يُمرَّر على `fix-glb-model.mjs` ويُخبَز الحجم داخل الملف، لا الاعتماد على
`normalizeScale()` في `main.js` وحدها. هذه الدالة مفيدة كشبكة أمان لفروقات صغيرة فقط.

### 6. ربط طبق حقيقي ثانٍ بنموذج 3D
"بيتزا البيبروني الفاخرة بالجبن الذائب" (`ac10a115-...`) أصبح لها نموذج
(`/firasse_resto/pizaa3d.glb`، ملف بيتزا موجود مسبقاً بأبعاد معقولة 0.92×0.41×0.84م، لم
يحتج تعديلاً). النتيجة: **3 من 5 أطباق حقيقية** لها الآن مجسّم 3D فعّال:
| الطبق | الملف | الحالة |
|---|---|---|
| بيتزا بيبروني فاخرة | `piza3d.glb` | يعمل (11.5MB — ثقيل، انظر "الباقي" أدناه) |
| حبار محشو مشوي | `calamari_fixed.glb` | يعمل، مصحَّح بالكامل هذه الجلسة |
| بيتزا البيبروني الفاخرة بالجبن الذائب | `pizaa3d.glb` | يعمل (966KB) |
| كروكيت الدجاج المقرمش | — | بلا نموذج (فقط صورة) |
| بيتزا نابولي الإيطالية | — | بلا نموذج (فقط صورة) |

### 7. التحقق النهائي
`node scripts/verify-view.mjs https://talabati.rest firasse_food` → **كل الفحوص ناجحة**،
بما فيها تنزيل الثلاثة ملفات GLB الفعلية من الإنتاج والتحقق من توقيعها.

### 8. Git
كل التعديلات (السكربتات + لا تعديل كود، فقط بيانات + ملفات GLB جديدة) مرفوعة على GitHub.

---

## ⏳ ما تبقى — لجلسة/جلسات قادمة (بالترتيب المُقترَح)

### أ) اختبار زر "ضَعه على طاولتك" على هاتف حقيقي — **الأهم، يتطلب صاحب الجهاز**
لم يُختبر على جهاز فعلي بعد (فقط تحقق تقني من الكود/الإعدادات في `public/view/index.html`:
`ar-modes="webxr scene-viewer quick-look"` موجود وصحيح شكلياً). يجب فتح الرابط فعلياً على:
- **أندرويد** (ARCore عبر Scene Viewer) — يجب أن يعمل لأن الملفات GLB فقط.
- **آيفون** (Quick Look) — **غالباً لن يعمل** لأن Quick Look يحتاج ملف `USDZ` وليس `GLB`
  مباشرة (model-viewer يحاول توليده تلقائياً من الموديل الحالي في المتصفح، لكن هذا التحويل
  التلقائي غير مضمون الجودة/النجاح دائماً، خصوصاً مع مواد معقدة). **لدعم آيفون بثقة: توليد
  USDZ مسبقاً لكل GLB ورفعه بجانبه** (أداة `usd_from_gltf` أو خدمة تحويل سحابية).

### ب) لوحة الأدمن — إمكانية ضبط rotation/scale موجودة في الكود لكن المستخدم لم يستطع إيجادها
تحقّقتُ: الحقول **موجودة فعلاً** في `public/admin/index.html` ضمن نافذة تعديل الطبق، داخل
`<fieldset><legend>3D Model Configuration</legend>` — تحتوي `Scale X/Y/Z`, `Position X/Y/Z`,
`Rotation X/Y/Z`, `Animation`. أي أن الكود يدعم التعديل، لكن صاحب المشروع لم يتمكن من
الوصول إليها عملياً. **يحتاج فحصاً في الجلسة القادمة:**
1. هل الحقول تظهر فعلاً في الواجهة عند فتح "Edit" على طبق موجود؟ (تحقق بصري في المتصفح)
2. هل تحتاج تمريراً (scroll) داخل النافذة المنبثقة (Modal) لرؤيتها؟ قد تكون المشكلة تجربة
   استخدام (UX) لا خللاً في الكود.
3. هل تُحفَظ القيم فعلاً عند الضغط على "Save"؟ (تتبّع `handleDishSubmit` في
   `public/admin/main.js`، القسم الذي يبني `model3D.rotation/scale`).
4. **تحسين مُقترَح:** إخفاء هذا القسم بالكامل إن لم يكن للطبق موديل مرفوع أصلاً (تجربة
   مستخدم أوضح)، أو نقله لأعلى النافذة إن كان مدفوناً تحت حقول أخرى كثيرة.

### ج) تحسين حجم ملف `piza3d.glb` (11.5MB — ثقيل جداً لبيانات الهاتف)
`verify-view.mjs` ينبّه بذلك تلقائياً كل مرة. الحل: تشغيله على `fix-glb-model.mjs` (يزيل
أي normal map زائف، يضغط، ويحذف صوراً غير مستخدمة من نهاية الـbuffer) — تماماً كما فعلنا
لملف الحبار. **لم يُنفَّذ بعد لأن الملف يعمل بصرياً حالياً ولم يكن ضمن طلب هذه الجلسة.**

### د) ربط نموذج 3D لبقية الأطباق (كروكيت الدجاج، بيتزا نابولي)
لا تملك نموذج GLB حالياً. إن لم يُرفع ملف مناسب لكل منهما، ستبقى تُعرض بصورهما الثنائية
فقط (وهذا مقبول تماماً لنسخة Beta، فقط تأكد صاحب المطعم يعرف ذلك مسبقاً).

### هـ) توليد USDZ لكل الموديلات (لدعم آيفون بثقة) — مرتبط بالبند (أ)

---

## 🧰 أدوات التشخيص المتاحة (استخدمها قبل أي تخمين)

| السكربت | الغرض |
|---|---|
| `node scripts/inspect-glb-local.mjs <path>` | يقرأ ملف GLB من القرص محلياً: المواد، الإحداثيات، **الصندوق المحيط الحقيقي بالمتر**، وتحذير تلقائي إن كان الموديل "واقفاً" (الارتفاع أكبر من القاعدة). |
| `node scripts/inspect-glb-scenegraph.mjs <path>` | يطبع شجرة العُقَد (nodes/scenes/children) — مفيد قبل تشغيل `fix-glb-model.mjs` على ملف معقّد. |
| `node scripts/fix-glb-model.mjs <in> <out> [--diameter 0.32]` | **الأداة الرئيسية لإصلاح أي موديل جديد:** يخبز الحجم الصحيح داخل الملف (لا يعتمد على JS)، يزيل normal maps مزيّفة، يحذف صوراً غير مستخدمة. **استخدمها دائماً عندما يحتاج الملف تصغيراً/تكبيراً بمعامل > 3×.** |
| `node scripts/list-dishes-full.mjs` | يطبع كل أطباق قاعدة البيانات مع حقل `model3d` كاملاً (url, scale, rotation, position). |
| `node scripts/verify-view.mjs https://talabati.rest firasse_food` | فحص شامل للإنتاج: الصفحة، الكاميرا، الـAPI، **وتنزيل كل ملف GLB فعلياً من الإنتاج والتحقق من توقيعه**. شغّله بعد أي نشر. |

**قاعدة ذهبية:** لا تُغيّر أي قيمة `scale`/`rotation` بالتخمين. افتح الملف بـ
`inspect-glb-local.mjs` أولاً واقرأ الأبعاد الحقيقية، كما فعلنا مع الحبار والبطة.

---

## معرّفات مفيدة (Firasse Food)

- `restaurant_id`: `b66900eb-919c-4200-a061-cdc7083ab43e`
- `menu_id`: `35253c43-916c-4f9a-8330-af29665a9b34`
- `slug`: `firasse_food`
- طبق الحبار: `caaecd76-875a-4298-b91d-1722f6b6ffbb`
- طبق بيتزا البيبروني بالجبن الذائب: `ac10a115-8e34-4c1f-bd78-1816ecf27cb9`
