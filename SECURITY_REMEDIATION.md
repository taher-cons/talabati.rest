# تقرير تنفيذ الإصلاحات الأمنية — طلباتي WebAR Menu
**التاريخ:** 2026-07-30 · **المرجع:** `ENGINEERING_AUDIT_REPORT.md`

هذا الملف يسجّل ما **نُفِّذ فعلياً** (لا توصيات)، وما تبقّى ويحتاج تدخّلك.

---

## 1. الثغرة الحرجة: قاعدة بيانات مفتوحة للعالم

المفاتيح كانت مسرّبة في المستودع (`.env`, `.env.txt`, `.env.production`)، وسياسات RLS
كانت `USING (true)` للكتابة، وكانت هناك دالة `exec_sql` تسمح بتنفيذ SQL عشوائي.
النتيجة: أي شخص لديه المفتاح العام يستطيع **الحذف والتعديل وتنفيذ SQL**.

### ما نُفِّذ

| # | الإجراء | الحالة |
|---|---------|--------|
| 1 | نسخة احتياطية كاملة قبل أي تغيير (`scripts/backup-db.sql`) | ✅ |
| 2 | حذف دالة `exec_sql` و `exec_sql_v2` (تنفيذ SQL عن بُعد) | ✅ |
| 3 | إعادة كتابة سياسات RLS: `anon` **قراءة فقط** للمحتوى المنشور، والكتابة لـ `service_role` فقط | ✅ |
| 4 | تفعيل RLS على كل الجداول بلا استثناء | ✅ |
| 5 | إصلاح سياسات Storage (رفع الملفات لم يبقَ عاماً) | ✅ |
| 6 | **تدوير المفاتيح** إلى نظام Supabase الجديد (`sb_publishable_…` / `sb_secret_…`) وإبطال القديمة | ✅ |
| 7 | حذف ملفات `.env*` المسرّبة من القرص وإضافتها إلى `.gitignore` | ✅ |
| 8 | تخزين المفاتيح في **Firebase Secret Manager** (4/4) بدل الملفات | ✅ |

### التحقق (تم اختباره فعلياً بالمفتاح العام من الخارج)
```
قراءة قائمة منشورة      → 200 OK      (المطلوب: يعمل)
INSERT / UPDATE / DELETE → 401/403     (المطلوب: مرفوض)
RPC exec_sql             → 404         (المطلوب: غير موجود)
```

---

## 2. طبقة الـ API (Cloud Functions)

| # | المشكلة | الإصلاح |
|---|---------|---------|
| 1 | مسارات `/admin/**` و `/upload/**` كانت **بلا أي مصادقة** | `requireAdmin` على كل الموجّهات الحساسة (`functions/src/middleware/auth.js`) |
| 2 | `/auth/signup` كان يسمح لأي شخص بإنشاء حساب `owner` | صار محمياً بمفتاح الإدارة |
| 3 | CORS لأصل واحد فقط (كان يحجب www و firebaseapp.com) | قائمة أصول مسموحة صريحة |
| 4 | لا حماية من هجمات تخمين كلمة المرور | 10 محاولات/15 دقيقة على مسارات الدخول |
| 5 | استخدام `Proxy` مع Supabase كان يفسد `this` | استبداله بدوال getter |
| 6 | الأخطاء كانت تُرجع 500 عامّاً بلا أثر | `x-request-id` لكل طلب + تسجيل مُهيكل + رموز 413 الصحيحة |
| 7 | `/api/health` لم يكن يفحص شيئاً | يفحص DB + Storage مع زمن الاستجابة، ويرجع **503** عند التدهور (لتُنبّه المراقبة) |
| 8 | `X-Frame-Options: DENY` كان يعطّل صفحات AR | `SAMEORIGIN` + CSP كامل + `Permissions-Policy: camera` |
| 9 | **كل** طلب لمطعم/قائمة/طبق غير موجود كان يرجع **500** (`.single()` يرمي خطأ عند صفر صفوف) — اكتشفه اختبار الدخان بعد النشر | `.maybeSingle()` في دوال القراءة الأربع → 404 نظيفة |


---

## 3. الواجهات (Frontend)

| # | المشكلة | الإصلاح |
|---|---------|---------|
| 1 | لوحة الإدارة لا ترسل أي مصادقة (ستُرفض بعد الإصلاح) | ترسل `x-admin-key`، تطلب المفتاح مرة واحدة وتحفظه في `sessionStorage` فقط، وتعيد السؤال عند 401 |
| 2 | رفع الشعار كان يقرأ `result.image.url` وهو **غير موجود** دائماً | صار `result.file.url` (كان الشعار يُحفظ فارغاً بصمت) |
| 3 | فشل تحميل موديل 3D يترك المستخدم أمام كاميرا فارغة | نموذج بديل (طبق + صورة الطبق) مع رسالة واضحة |
| 4 | `anim.stop()` غير موجود في GSAP → استثناء عند تبديل الأطباق | `anim.kill()` (كان يسبب تسريب ذاكرة) |
| 5 | `y: '+=2*Math.PI'` لا تفهمها GSAP → دوران معطّل | `'+=6.28318'` |
| 6 | لوحة الإدارة تعرض "MongoDB URI" والمشروع على Supabase | نص صحيح: Supabase URL/Key |

---

## 4. البنية التحتية والجودة

- `firebase.json`: ترتيب التوجيهات مصحّح (كان `**` يبتلع `/api/**`)، تخزين مؤقت مناسب للموديلات، إزالة إشارة `storage.rules` غير المستخدمة.
- `public/robots.txt` + `public/sitemap.xml`: منع فهرسة `/admin` و `/api` و `/ar`.
- `scripts/smoke-test.mjs`: 10 اختبارات تتحقق أن الكتابة بلا مصادقة **مرفوضة** فعلاً على الموقع الحقيقي.
- `.github/workflows/ci.yml`: بناء + فحص، و**حاجز يفشل البناء** إذا عاد أي مفتاح Supabase إلى المستودع أو صار `.env` مُتتبَّعاً.
- سكربتات: `npm run secrets:rotate` / `secrets:push` / `test:smoke`.

---

## 5. ما تبقّى ويحتاج تدخّلك (لا يمكن تنفيذه آلياً)

### 🔴 عالي الأهمية

1. **تنظيف تاريخ Git من المفاتيح المسرّبة**
   المفاتيح القديمة أُبطلت، لكنها لا تزال في تاريخ الـ commits. إن كان المستودع عاماً:
   ```bash
   pip install git-filter-repo
   git filter-repo --invert-paths --path .env --path .env.txt --path .env.production
   git push --force --all
   ```
   ⚠️ يُعيد كتابة التاريخ — أبلغ أي شخص آخر يعمل على المستودع.

2. **تدوير `service_role` القديم في Supabase Dashboard**
   إن كان مفتاح JWT القديم (`eyJ…`) لا يزال ظاهراً في
   *Settings → API Keys → Legacy keys* فاضغط **Disable/Revoke** عليه صراحةً.

3. **حدّد لمن مفتاح `ADMIN_API_KEY`**
   المفتاح الجديد في `.env` محلياً. لا ترسله عبر بريد أو محادثة غير مشفّرة.

### 🟡 مستحسن

4. **تنبيهات المراقبة**: أنشئ Uptime Check على `https://talabati.rest/api/health`

   (يرجع 503 عند تدهور القاعدة) + تنبيه على معدل أخطاء 5xx.
5. **نسخ احتياطي مجدول** لقاعدة Supabase (الخطة المجانية لا تضمن استرجاعاً زمنياً).
6. **استبدال مفتاح الإدارة بمصادقة Supabase Auth كاملة** (JWT + أدوار) — المفتاح المشترك حلٌّ مؤقّت جيد لكنه غير قابل للتوسّع مع عدة مطاعم.
7. **رفع تغطية الاختبارات**: لا توجد اختبارات وحدة بعد؛ اختبارات الدخان تغطي الأمان فقط.

---

## 6. أوامر التحقق السريع

```bash
npm run test:smoke                 # يتأكد أن الكتابة بلا مصادقة مرفوضة
curl https://talabati.rest/api/health
npm run secrets:push               # إعادة رفع المفاتيح إلى Secret Manager
```
